
/**
 * Finish the onboarding process, if not yet complete.
 */
// [ONBOARD]export default function ({ store, redirect, route, error, req }) {
// [ONBOARD]const routeName = 'register-complete'

// [ONBOARD]if (store.getters['auth/activeUser'] && (route.name !== routeName) && ((store.getters['meta/isResearcher'] || store.getters['meta/isSourcerer']) && !(store.getters['meta/onboardingComplete']))) {
// [ONBOARD]    return redirect({ name: routeName })
// [ONBOARD]}
// [ONBOARD]}
